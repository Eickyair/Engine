{
  "info": {
    "name": "Traffic Engine API Minimal",
    "description": "Minimal Postman collection for the current FastAPI HTTP endpoints exposed by the traffic engine. WebSocket streaming is documented in src/traffic_engine/api/README.md.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "_postman_id": "f6c5c3c4-4db0-4d84-a05c-2d6ff6940f5d"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://127.0.0.1:8000"
    },
    {
      "key": "area_id",
      "value": "roma-norte"
    },
    {
      "key": "simulation_id",
      "value": "sim-001"
    }
  ],
  "item": [
    {
      "name": "Health",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/health",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "health"
          ]
        },
        "description": "Checks that the API is up."
      }
    },
    {
      "name": "OpenAPI Schema",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/openapi.json",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "openapi.json"
          ]
        },
        "description": "Returns the complete OpenAPI schema generated by FastAPI."
      }
    },
    {
      "name": "Endpoint Schemas Document",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/api/response-models.json",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "api",
            "response-models.json"
          ]
        },
        "description": "Returns a shareable JSON document with inline request and response schemas for each HTTP endpoint."
      }
    },
    {
      "name": "List Geographic Areas",
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "if (pm.response.code === 200) {",
              "  const payload = pm.response.json();",
              "  if (Array.isArray(payload) && payload[0]?.area_id) pm.collectionVariables.set('area_id', payload[0].area_id);",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/geographic-areas",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "geographic-areas"
          ]
        },
        "description": "Returns the geographic areas that can be used to start simulations."
      }
    },
    {
      "name": "Get Geographic Area Topology",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/geographic-areas/{{area_id}}/topology",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "geographic-areas",
            "{{area_id}}",
            "topology"
          ]
        },
        "description": "Returns the topology for the current area_id variable, including nodes, edges, lanes and bounding box."
      }
    },
    {
      "name": "Create Simulation",
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "if (pm.response.code === 201) {",
              "  const payload = pm.response.json();",
              "  if (payload.simulation_id) pm.collectionVariables.set('simulation_id', payload.simulation_id);",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"area_id\": \"{{area_id}}\",\n  \"initial_vehicles\": 25,\n  \"max_vehicles\": 60,\n  \"max_steps\": 120,\n  \"spawn_rate\": 0.2,\n  \"noise_prob\": 0.3,\n  \"seed\": 42,\n  \"tick_interval_ms\": 100,\n  \"execution_mode\": \"continuous\",\n  \"default_lanes\": 1,\n  \"traffic_light_percentage\": 0.0,\n  \"traffic_light_green_steps\": 10,\n  \"traffic_light_red_steps\": 10,\n  \"enable_lane_changes\": false\n}"
        },
        "url": {
          "raw": "{{baseUrl}}/simulations",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "simulations"
          ]
        },
        "description": "Creates a simulation using the current CreateSimulationRequest schema and stores the returned simulation_id as a collection variable."
      }
    },
    {
      "name": "Get Simulation",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/simulations/{{simulation_id}}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "simulations",
            "{{simulation_id}}"
          ]
        },
        "description": "Fetches the simulation record for the current simulation_id variable."
      }
    },
    {
      "name": "List Simulation Steps",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/simulations/{{simulation_id}}/steps",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "simulations",
            "{{simulation_id}}",
            "steps"
          ]
        },
        "description": "Lists stored steps for the current simulation. This returns 409 until the simulation is ready."
      }
    },
    {
      "name": "Cancel Simulation",
      "request": {
        "method": "POST",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/simulations/{{simulation_id}}/cancel",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "simulations",
            "{{simulation_id}}",
            "cancel"
          ]
        },
        "description": "Requests cancellation for the current simulation."
      }
    }
  ]
}